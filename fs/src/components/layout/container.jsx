const Container = ({ children }) => (
    <main className="min-h-[calc(100dvh-72px)] w-full bg-[#f0f4f8] flex items-stretch md:pl-56 lg:pl-64">
        {children}
    </main>
);

export default Container;
